{% block sw_product_image_context_cover_action %}
    {% parent %}

    {% block sw_product_image_context_flip_action %}
        <sw-context-menu-item
            v-if="!isFlip && !isCover"
            class="sw-product-image__button-cover"
            @click="$emit('sw-product-image-flip')"
        >
            Use as flip
        </sw-context-menu-item>
        <sw-context-menu-item
            v-if="isFlip && !isCover"
            class="sw-product-image__button-cover"
            variant="danger"
            @click="$emit('sw-product-image-flip-delete')"
        >
            Remove flip
        </sw-context-menu-item>
    {% endblock %}

    {% block sw_product_image_context_context_photo_action %}
        <sw-context-menu-item
            v-if="!isContextPhoto && !isCover"
            class="sw-product-image__button-cover"
            @click="$emit('sw-product-image-context-photo')"
        >
            Use as context photo
        </sw-context-menu-item>
        <sw-context-menu-item
            v-if="isContextPhoto && !isCover"
            class="sw-product-image__button-cover"
            variant="danger"
            @click="$emit('sw-product-image-context-photo-delete')"
        >
            Remove context photo
        </sw-context-menu-item>
    {% endblock %}
{% endblock %}


{% block sw_product_image_preview %}
    {% parent %}

    <sw-label
        v-if="isFlip && !isCover"
        class="sw-product-image__flip-label"
        variant="primary"
        size="medium"
        appearance="pill"
    >
        Flip
    </sw-label>

    <sw-label
        v-if="isContextPhoto && !isCover"
        class="sw-product-image__context-photo-label"
        variant="info"
        size="medium"
        appearance="pill"
    >
        Context
    </sw-label>
{% endblock %}
